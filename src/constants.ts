import type { Theme } from './types'

const BRAND_NAME: string = 'Mercado Play'

const DEFAULT_THEME: Theme = 'dark'
const DEFAULT_THEME_STORAGE_KEY: string = 'ui-theme'

export { BRAND_NAME, DEFAULT_THEME, DEFAULT_THEME_STORAGE_KEY }
